# =====================================================================
# MUTT v2.3 Production Dependencies
# =====================================================================
# Install with: pip install -r requirements.txt
# =====================================================================

# Web Framework
Flask==3.1.2
gunicorn==21.2.0

# Database
psycopg2-binary==2.9.11

# Redis
redis==5.0.1

# Vault
hvac==2.0.0

# SNMP trap sending for demo tooling
pysnmp==4.4.12

# HTTP Client
requests==2.32.5

# Monitoring
prometheus-client==0.23.1
prometheus-flask-exporter==0.23.0

# =====================================================================
# Observability (Phase 2 - Optional)
# =====================================================================
# OpenTelemetry tracing (opt-in via OTEL_ENABLED=true)
# These are optional - services work without them installed
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0

# Auto-instrumentation libraries (optional)
opentelemetry-instrumentation-flask>=0.45b0
opentelemetry-instrumentation-requests>=0.45b0
opentelemetry-instrumentation-redis>=0.45b0
opentelemetry-instrumentation-psycopg2>=0.45b0

# =====================================================================
# Version Notes:
# =====================================================================
# - Flask 2.3.3: Stable, production-ready
# - redis 5.0.1: Matches RHEL 8 Redis version
# - psycopg2-binary: PostgreSQL adapter (binary for easier install)
# - hvac 2.0.0: Vault client with token renewal support
# - prometheus-client: Core metrics library
# - prometheus-flask-exporter: Flask integration for metrics
# - opentelemetry-*: Python 3.10+ compatible, opt-in distributed tracing
# =====================================================================
